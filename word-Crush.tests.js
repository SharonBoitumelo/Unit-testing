describe('Testing the Word-Crush function' , function(){
    it('the moment you enter the sentence, it shoult return the longest word' , function(){
       assert.equal(2,2);

        assert.deepEqual([2,2],[2,2]);
    });
    it('the moment you enter the sentance, it should return the shortest word' , function(){
       assert.equal(2,2);

        assert.deepEqual([2,2],[2,2]);
    });
    it('the moment you enter the sentance, it should return the number/sum of the sentance' , function(){
        assert.equal(2,2);
 
         assert.deepEqual([2,2],[2,2]);
     });
});